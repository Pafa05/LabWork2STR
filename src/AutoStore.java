public class AutoStore {
    private Pallet pallet;
    private Mechanism mechanism;

    public AutoStore(Pallet pallet, Mechanism mechanism) {
        this.pallet = pallet;
        this.mechanism = mechanism;
    }
}
